# Test Case: Hq_Commu_00002 본사 공지사항 등록 관리

본 문서는 **본사 공지사항 등록 관리 (hq_commu_00002)** 화면의 단위 테스트 시나리오 및 E2E 자동 검증 결과를 기술합니다.

## 1. 테스트 기본 정보
* **테스트 대상 화면**: 공지사항 등록 관리 (`hq_commu_00002`)
* **테스트 접속 계정**: `7249525SHOP` (본사 최상위 관리자 권한)
* **테스트 접속 비밀번호**: `0000` (E2E 수행용 임시 패스워드)
* **DB 검증 환경**: IP: `192.168.10.206` / DB: `edb`

---

## 2. 테스트 사전 조건
1. 로컬 톰캣 서버가 정상적으로 기동되어 있고 `http://localhost:8080`에 접속 가능해야 합니다.
2. `7249525SHOP` 계정이 활성화 상태여야 합니다.
3. 테스트 전용 임시 데이터의 정합성 유지를 위해, 제목이 `Auto QA Notice`인 공지사항 데이터는 DB에서 사전 삭제 처리됩니다.

---

## 3. 단위 테스트 시나리오 목록

| ID | 분류 | 테스트 시나리오 | 예상 결과 | 판정 |
|---|---|---|---|---|
| TC-01 | 조회 | 조회조건 없이 '조회' 버튼 클릭 | 등록된 공지사항 목록이 그리드(`hq_commu_00002_t01`)에 최신순 정상 로드됨 | **PASS** |
| TC-02 | 조회 | 제목 검색 조건 입력 후 '조회' 클릭 | 입력한 제목을 포함하는 공지사항 목록만 필터링되어 노출됨 | **PASS** |
| TC-03 | 등록 | '새 글쓰기' 버튼 클릭 | 새 글쓰기 모달(`hq_commu_00002_M01`)이 팝업됨 | **PASS** |
| TC-04 | 등록 | 필수값(공지기간, 제목, 내용) 미입력 후 저장 시도 | 입력 오류 알림 Bootbox가 노출되며 저장이 반려됨 | **PASS** |
| TC-05 | 등록 | 공지사항 저장 및 다중 첨부파일 업로드 | 1) 공지글 텍스트 저장 성공 (DB `BBSNTCTB` 적재)<br>2) 업로드 파일 물리 저장소 저장 및 매핑 정보 DB(`BBSNTUTB`) 적재 성공<br>3) 모달이 정상 종료되고 그리드 리프레시됨 | **PASS** |
| TC-06 | 상세 | 그리드 행 클릭 | 상세조회 및 수정 모달(`hq_commu_00002_M03`)이 팝업되며 작성된 제목, 내용, 기등록 첨부파일 목록이 바인딩됨 | **PASS** |
| TC-07 | 수정 | 상세조회 모달에서 내용 수정 후 저장 | 공지글 텍스트 및 신규 첨부파일 정보가 정상 업데이트 및 DB 반영됨 | **PASS** |
| TC-08 | 삭제 | 상세조회 모달에서 '삭제' 버튼 클릭 및 확인 | 게시글 데이터(`BBSNTCTB`) 및 매핑된 첨부파일 정보(`BBSNTUTB`)가 일괄 물리 삭제(Cascade) 처리됨 | **PASS** |

---

## 4. 브라우저 E2E 테스트 수행 결과 (Playwright)

### 4.1 E2E 테스트 개요
* **수행 도구**: Playwright (Async Python API)
* **테스트 스크립트**: [test_hq_commu_02.py](file:///D:/hmTest/backoffice/QaReport/test_hq_commu_02.py)
* **수행 일시**: 2026-06-29
* **주요 검증 동작**:
  1. `7249525SHOP` 계정 자동 로그인 처리
  2. 공지사항 등록 관리 화면 이동
  3. 새 글쓰기 팝업 후 공지기간 설정 및 제목, 본문(Summernote) 기입
  4. 테스트용 더미 텍스트 파일(`qa_test_file.txt`) 첨부 후 등록 저장 요청
  5. DB (`BBSNTCTB`, `BBSNTUTB`) 연쇄 등록 검증 (IDX = `99859`, FILE_IDX = `296`)
  6. 그리드 내 정상 출력 확인 및 최종 화면 캡처 (`hq_commu_00002_search.png`)
  7. 상세조회 모달을 통한 연쇄 삭제(Cascade Delete) 동작 검증
  8. DB 데이터 완전 삭제 검증 및 세션 로그아웃/복구

### 4.2 최종 판정 및 증적
* **E2E 테스트 판정**: **PASS**
* **테스트 증적 스크린샷**: [hq_commu_00002_search.png](file:///D:/hmTest/backoffice/QaReport/hq_commu_00002_search.png)
