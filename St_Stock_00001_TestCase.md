# Test Case: St_Stock_00001 매장 재고 조정 등록

**대상 화면**: 재고관리 > 조정/폐기/실사 > 조정등록 (`st_stock_00001`)  
**연계 화면**: [조정/실사 현황] (`st_stock_00002`), [현재고조회] (`st_stock_00007`)  
**작성일**: 2026-06-05  
**작성자**: AI QA Agent (Antigravity)  

---

## 1. 테스트 기본 정보
* **테스트 계정**: `fnbcafe` / `0000` (매장 코드: `NC0007`, 본사 코드: `NC0002` 바인딩)
* **테스트 대상 상품**: `T0000001` (Food그레놀라 치즈, 입수량: 1, 기준원가: 35,000원) / `T0000555` (클라우드 병맥주)

---

## 2. 테스트케이스 정의서 (UI 및 기능 검증)

| TC ID | 테스트 대분류 | 테스트 시나리오 / 수행 절차 | 입력 데이터 | 예상 결과 (Expected Result) | 판정 |
|---|---|---|---|---|---|
| **TC_ST_001_01** | 화면 진입 | 1. `fnbcafe` 계정 로그인<br>2. `재고관리 > 조정/폐기/실사 > 조정등록` 메뉴 진입 | N/A | 화면이 에러 없이 로딩되며, 기본 조회 일자가 오늘 날짜(2026-06-05)로 자동 세팅된다. | **PASS** |
| **TC_ST_001_02** | 상품 검색 모달 | 1. 메인 화면의 `[등록]` 버튼 클릭<br>2. 상품 추가 팝업 오픈 확인<br>3. 상품코드 입력 후 `[조회]` 클릭 | 상품코드: `T0000555` | 팝업 내 그리드에 해당 상품의 정보(상품코드, 상품명, 규격, 현재고 등)가 정확하게 1건 조회된다. | **PASS** |
| **TC_ST_001_03** | 그리드 추가 | 1. 팝업에서 조회된 상품의 체크박스 선택<br>2. `[선택]` 버튼 클릭 | N/A | 선택한 상품이 메인 화면의 조정 그리드에 추가되며 팝업이 닫힌다. | **PASS** |
| **TC_ST_001_04** | 수량 자동 연산 | 1. 추가된 상품의 `조정수량(낱개)` 필드에 수량 입력<br>2. 포커스 아웃 | 수량: `10` | 입력된 낱개 수량과 원가에 따라 조정금액(수량 * 원가)이 그리드 상에 자동 계산되어 표시된다. | **PASS** |
| **TC_ST_001_05** | 사유 및 비고 입력 | 1. 해당 로우의 `조정사유` 콤보박스 선택<br>2. `비고` 입력 | 사유: `003` (기타조정)<br>비고: `QA Test` | 콤보박스에서 선택한 조정사유 코드와 입력한 비고가 그리드에 정상적으로 반영된다. | **PASS** |
| **TC_ST_001_06** | 임시 저장 (DML) | 1. 해당 상품 체크박스 선택<br>2. `[저장]` 버튼 클릭 | N/A | 1. 저장 완료 토스트 알림창이 팝업된다.<br>2. DB `IMREALTB`에 데이터가 생성되며 처리 상태(`PROC_FG`)가 `'0'`(등록/임시)으로 확인된다. | **PASS** |
| **TC_ST_001_07** | 조정 확정 처리 | 1. 저장된 로우 선택<br>2. `[확정]` 버튼 클릭 | N/A | 1. 확정 완료 알림이 노출된다.<br>2. DB `IMREALTB`의 처리 상태(`PROC_FG`)가 `'1'`(확정)로 변경된다.<br>3. 수불 대기 테이블(`IMTRLGTB`)에 수불 유형 `'A'`(조정) 로그가 정상 적재된다. | **PASS** |
| **TC_ST_001_08** | 조정 삭제 처리 | 1. 신규 상품 등록 및 저장(임시저장) 진행<br>2. 해당 로우 체크 후 `[삭제]` 버튼 클릭 | N/A | 그리드에서 해당 상품이 사라지고, DB `IMREALTB` 테이블에서 해당 데이터가 물리적으로 DELETE된다. | **PASS** |

---

## 3. 테스트케이스 정의서 (DB 및 배치 프로세스 연동 검증)

| TC ID | 테스트 대분류 | 테스트 시나리오 / 수행 절차 | 입력 데이터 | 예상 결과 (Expected Result) | 판정 |
|---|---|---|---|---|---|
| **TC_ST_001_09** | 새벽 배치 연동 | 1. `confirmReal`(확정) 완료 후 수불 대기 로그 적재 확인<br>2. 야간 배치 프로그램 실행 (또는 수동 시뮬레이션 SQL 실행) | 대상 매장: `NC0007`<br>대상 상품: `T0000001` | 1. 수불 대기 테이블(`IMTRLGTB`)에서 로그가 삭제되고 백업 테이블(`IMTRBKTB`)로 이관된다.<br>2. 현재고 테이블(`IMCRIOTB`)의 현재고(`CUR_QTY`)가 조정값 수치(예: 400개)로 최종 갱신된다. | **PASS** |
| **TC_ST_001_10** | 연계 화면 - 이력 | 1. `st_stock_00002` (조정/실사 현황) 화면 진입<br>2. 대상 일자(2026-06-05) 조건으로 조회 | 매장: `NC0007` | 확정 처리했던 각각의 개별 조정 전표 이력(예: 1000개 건, 400개 건)이 총 2건의 라인으로 정상적으로 리스팅된다. | **PASS** |
| **TC_ST_001_11** | 연계 화면 - 재고 | 1. `st_stock_00007` (현재고조회) 화면 진입<br>2. 대상 상품 검색 | 상품코드: `T0000001` | 실시간 계산 방식에 의해 재고 수량 `400`, 재고 단가 `35,000`, 재고 금액 `14,000,000`원이 정상적으로 계산되어 출력된다. | **PASS** |

---

## 4. 특이사항 및 검증 결과 코멘트
1. **무한 루프 버그 조치**: 
   * 서비스단 `confirmReal` 동작 시 자바 루프 변수 오동작(`j` 루프 안에서 `i++` 사용)으로 발생하던 WAS 행(Hang) 현상이 정상 패치 및 검증되었습니다. (`TC_ST_001_07` 검증 완료)
2. **화면 조회 일관성**:
   * 재고 조정이 중복해서 연속으로 일어날 때, `st_stock_00002`는 **'전표 이력'**을 보여주는 화면이므로 2건이 각각 보이는 것이 정상이고, `st_stock_00007`은 **'현재고 상태'**를 실시간으로 환산하여 뿌려주므로 최종 400개 / 14,000,000원이 정확히 일치하여 정상 판정되었습니다.
