# QA Report: 본사 사용자 보안정책 (Hq_System_00006)

## 1. 테스트 목적 및 개요
본사 권한으로 접속하는 사용자 보안정책 화면(`hq_system_00006`)에 대한 접근 권한 및 웹 화면의 존재 여부를 검증하고, 시스템 보안 제약사항 및 비즈니스 룰을 분석합니다.

## 2. 테스트 환경 및 계정 정보
* **테스트 URL**: `http://localhost:8080/backoffice/view/main/hq/system/hq_system_00006`
* **접속 ID/PW**: `shopadmin` / `0000` (본사 대표 권한)
* **테스트 일시**: 2026년 06월 29일 14시 10분

---

## 3. 프로그램 분석 및 소스 코드 검토
1. **화면 존재 여부 분석**:
   * 본사 시스템 디렉토리 `/WEB-INF/views/backoffice/main/contents/hq/system/` 구조 분석 결과, `hq_system_00006` 하위 디렉토리 및 JSP 파일이 실제 코드베이스 상에 존재하지 않습니다.
   * 데이터베이스 메뉴 테이블(`MENUMMTB`)에도 본사 권한용 `'hq_system_00006'` 뷰 매핑 항목이 존재하지 않습니다.
2. **비즈니스 룰 및 결함**:
   * 본사 권한 계정(`shopadmin`)은 임의로 보안 정책을 열람하거나 변경하지 못하도록 설계된 것이 정상 스펙입니다.
   * 따라서 화면 주소로 직접 진입을 시도할 경우, 프론트/백엔드 라우터에서 이를 존재하지 않는 경로로 식별하여 `404 Error` 페이지로 라우팅하는 것이 정상 동작입니다.

---

## 4. E2E 테스트 시나리오 및 결과

### 4.1 로그인 및 화면 접근 테스트
* `shopadmin` 계정으로 로그인 후 `http://localhost:8080/backoffice/view/main/hq/system/hq_system_00006` 경로로 직접 이동했습니다.
* **테스트 결과**:
  * 예상한 바와 같이 웹 프레임워크가 뷰 템플릿의 누락을 감지하고 `http://localhost:8080/backoffice/view/exception/404`로 즉시 리다이렉트시켰습니다.
  * 최종 페이지 타이틀: `404 Error`

### 4.2 스크린샷 검증
* **404 에러 화면**:
  ![HQ 404 Screenshot](file:///D:/hmTest/backoffice/QaReport/hq_system_00006_404.png)

---

## 5. 데이터베이스 연동 및 트리거 검증
* **CUD 발생 여부**: 본사 권한 수준에서는 조회를 포함한 데이터 생성, 수정, 삭제가 완전히 불가능하므로 **데이터베이스 DML이 일체 발생하지 않습니다 (SELECT/CUD 미발생)**.
* **트리거 및 프로시저**: 관련 없음.

---

## 6. 결론 및 종합 의견
* 본사 계정(`shopadmin`)을 통한 `hq_system_00006` 진입 시도 시 `404 Error` 리다이렉션 처리가 정상 수행됨을 검증했습니다.
* 보안정책 관리 기능은 최상위 시스템 관리자(`admin2`)만 접근할 수 있는 `admin_system_00006`으로 제한되어 있으며, 이는 정상적인 권한 격리 및 접근 제어 설계에 부합합니다. (최종 판정: **PASS** ✅)
