# Test Case: St_Stock_00011 매장 점간이동 현황 조회 테스트

**대상 화면**: 
* 매장: 재고관리 > 점간이동 > 점간이동 현황 (`st_stock_00011`)
**연계 매장 정보**:
* 로그인 매장: `NC0003` (고양 Shop) - 사용자 계정: `shopbrand`
**작성일**: 2026-06-09  
**작성자**: AI QA Agent (Antigravity)  

---

## 1. 테스트 기본 정보
* **테스트 목적**: 매장(점포)의 관점에서 자점에서 등록하거나 수신한 점간이동 전표 목록과 상세 내역이 기간 및 필터 조건에 따라 정확하게 조회되는지 검증한다.
* **사용 계정 및 비밀번호**: 
  - 매장 관리자: `shopbrand` / `0000` (NC0003)
* **테스트 대상 일자**: 2026-06-01 ~ 2026-06-30 (실데이터가 존재하는 기간)

---

## 2. 테스트케이스 시나리오 (UI 및 기능 검증)

| TC ID | 테스트 단계 | 테스트 수행 절차 | 입력 데이터 | 예상 결과 (Expected Result) | 판정 |
|---|---|---|---|---|---|
| **TC_ST_011_01** | 화면 접속 및 기본 로딩 | 1. 매장 관리자 계정(`shopbrand`)으로 로그인<br>2. `st_stock_00011` 화면 진입<br>3. 조회조건 기본값 및 그리드 로딩 상태 확인 | N/A | 1. 점간이동 현황 화면이 에러 없이 정상 로딩된다.<br>2. 조회조건(등록일자, 전표구분, 보내는/받는 매장) 필드가 정상적으로 화면에 표출된다. | **PASS** |
| **TC_ST_011_02** | 등록일자 조건 검색 | 1. 등록일자 범위를 `2026-06-01` ~ `2026-06-30`로 설정<br>2. 우측 상단 `[조회]` 버튼 클릭 | 시작일: `2026-06-01`<br>종료일: `2026-06-30` | 1. 입력한 날짜 범위 내에 발생한 점간이동 전표 목록이 상단 그리드(`t01`)에 정상 조회된다.<br>2. 전표번호, 등록일자, 이출/이입 매장 정보, 총이동수량, 공급가, VAT, 합계 등의 컬럼 정보가 알맞게 표출된다. | **PASS** |
| **TC_ST_011_03** | 전표구분 필터링 검색 | 1. 전표구분을 `이출` 또는 `이입`으로 변경<br>2. `[조회]` 버튼 클릭 | 전표구분: `이출` (RECEIVE) 또는 `이입` (SEND) | 1. 선택한 구분값에 해당하는 전표들만 필터링되어 그리드(`t01`)에 노출된다. | **PASS** |
| **TC_ST_011_04** | 전표 상세 내역 조회 | 1. 조회된 목록 중 특정 전표의 **전표번호(Link)**를 클릭<br>2. 하단 상세 내역 그리드(`t02`) 조회 결과 확인 | N/A | 1. 클릭한 전표의 상세 품목 정보가 하단 그리드(`t02`)에 실시간으로 바인딩된다.<br>2. 상세 품목의 상품코드, 상품명, 규격, 입수, 단가, 이동총수량, 이동재고원가 등이 정상 표출된다. | **PASS** |

---

## 3. DB 연쇄 작용 검증 (DB Trigger / Procedure)

| TC ID | 테스트 대분류 | 테스트 시나리오 / 수행 절차 | 입력 데이터 | 예상 결과 (Expected Result) | 판정 |
|---|---|---|---|---|---|
| **TC_DB_011_05** | DB 트리거/프로시저 연쇄 작용 | 1. 본 화면은 Read-only 현황 조회 화면이므로 자체적인 DML(Insert/Update/Delete) 작용이 없음.<br>2. 점간이동 등록/확정 화면(`hq_stock_00011`/`st_stock_00010`)에서 발생시킨 DB 트리거/프로시저 결과 데이터가 정합성 있게 표출되는지 조회 검증 수행. | N/A | 1. 전표 상세 내역이 EDB DB 테이블(`MGMVHDTB`, `MGMVDTTB`, `TGOODSTB` 등)에 기록된 값과 일치하게 화면에 정확히 렌더링된다. | **PASS** |

---

## 4. 테스트 증적 자료 (Screenshots)

자동화 테스트 스크립트([test_st_stock_11.py](file:///d:/hmTest/backoffice/QaReport/test_st_stock_11.py)) 실행 결과 생성된 각 단계별 증적 자료입니다.

* **[1] 등록일자 조회 목록 출력 (st_stock_00011_search.png)**
  ![1. 조회 목록](file:///D:/hmTest/backoffice/QaReport/st_stock_00011_search.png)
  
* **[2] 전표 상세 내역 로드 (st_stock_00011_detail.png)**
  ![2. 상세 내역](file:///D:/hmTest/backoffice/QaReport/st_stock_00011_detail.png)
