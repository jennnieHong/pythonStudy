# TestCase: 사용자 보안정책 (hq_system_00006)

가맹점/본부 백오피스 내 본사 권한용 사용자 보안정책 조회 화면(`hq_system_00006`)의 E2E 테스트 케이스 정의서입니다.

---

## 1. 테스트 기본 정보
* **화면 ID**: `hq_system_00006`
* **메뉴 경로**: 시스템관리 > 영업정보시스템 > 사용자 보안정책 (본사 권한으로 접근 불인정)
* **테스트 계정**: `shopadmin` / `0000` (본사 대표 계정)

---

## 2. 테스트 시나리오 및 결과

| ID | 테스트 항목 | 입력 값 / 조건 | 예상 결과 | 판정 |
|:---|:---|:---|:---|:---|
| 1-1 | **미존재 화면 접근 시도** | `shopadmin` 계정으로 로그인 후 `http://localhost:8080/backoffice/view/main/hq/system/hq_system_00006` 주소로 직접 접근 | 존재하지 않는 뷰 파일이므로 웹 서버에서 감지하여 `/backoffice/view/exception/404` 페이지로 강제 리다이렉트 처리 | **PASS** |

---

## 3. 종합 의견 및 특이사항
* **화면 미존재**: 
  데이터베이스 메뉴 테이블(`MENUMMTB`) 및 실제 JSP 소스 코드 구조 분석 결과, 본사 전용 화면인 `hq_system_00006`은 존재하지 않는 메뉴(404 에러 발생)로 분류됩니다.
  사용자 보안정책 기능은 오직 시스템 관리자 권한을 가진 `admin_system_00006` 화면을 통해서만 조작할 수 있도록 제한되어 있습니다.
